@page
@using Volo.Abp.AspNetCore.Mvc.UI.Bootstrap.TagHelpers.Modal
@model Indo.Web.Pages.TransferOrder.HelpModel
@{
    Layout = null;
}
<abp-modal>
    <abp-modal-header title="About Inter-Warehouse Transfer"></abp-modal-header>
    <abp-modal-body>
        <abp-container>

            <h3>Inter-Warehouse Transfer</h3>
            <p>
                Inter-Warehouse Transfer is a Transfer order that request the moving of products from one warehouse to another.
            </p>
            <p>
                This module provide feature to manage Inter-Warehouse Transfer.
            </p>
            <h4>Document Status</h4>
            <ul>
                <li><b>Draft</b>.</li> Default status for the newly created transaction. Transaction can be update and deleted.
                <li><b>Confirm</b>.</li> Next status above Draft which is used for confirmed transaction. Transaction can not updated and can not deleted.
                <li><b>Returned</b>.</li> Default status for returned transaction. Only confirmed transaction can be returned. Transaction can not updated and can not deleted.
            </ul>
            <h4>Toolbar Actions</h4>
            <ul>
                <li><b>Help</b>. Show help guidance modal screen.</li>
                <li><b>Excel Export</b>. Export the data on grid to excel.</li>
                <li><b>Search Box</b>. Search the data on grid.</li>
                <li><b>Add</b>. Show modal screen to add new data.</li>
                <li><b>Edit</b>. Show modal screen to update selected data. Only active for draft transaction.</li>
                <li><b>Delete</b>. Show confirmation dialog to delete selected data. Only active for draft transaction.</li>
                <li><b>Details</b>. Navigate to detail view page for selected data.</li>
                <li><b>Confirm Transfer</b>. Show confirmation dialog to confirm selected data. Only active for draft transaction.</li>
                <li><b>Return Transfer</b>. Show confirmation dialog to return selected data. Only active for confirm transaction.</li>
                <li><b>Delivery Lookup</b>. Show modal screen that display the transfer delivery order (outbond).</li>
                <li><b>Receipt Lookup</b>. Show modal screen that display the transfer goods receipt (inbound).</li>
            </ul>
            <h4>Columns</h4>
            <ul>
                <li><b>Number</b>. Autogenerate specific number sequence based on module.</li>
                <li><b>Description</b>. Record additional description about the transfer.</li>
                <li><b>From Warehouse</b>. Lookup to warehouse as source warehouse.</li>
                <li><b>To Warehouse</b>. Lookup to warehouse as destination warehouse.</li>
                <li><b>Transfer Date</b>. Date of transfer.</li>
            </ul>
            <h3>Inter-warehouse Transfer Detail</h3>
            <p>
                Transfer order detail is a page that show detail view of the inter-warehouse transfer. This page provide feature to manage transfered items. Only draft transaction can be manage for addition, update and deletion.
            </p>
            <h4>Button Actions</h4>
            <ul>
                <li><b>Plus New Button</b>. Show modal screen to add new data.</li>
                <li><b>List Button</b>. Navigate back to main grid data.</li>
                <li><b>Print Button</b>. Navigate to windows print option (save as pdf, or print the detail view).</li>
            </ul>
            <h4>Toolbar Actions</h4>
            <li><b>Edit</b>. Show modal screen to update selected data. Only active for draft transaction.</li>
            <li><b>Delete</b>. Show confirmation dialog to delete selected data. Only active for draft transaction.</li>
            <h4>Columns</h4>
            <ul>
                <li><b>Product</b>. Detail transfered items as physical product that lookup to the product master data.</li>
                <li><b>Quantity</b>. Quantity of the item.</li>
            </ul>
        </abp-container>
    </abp-modal-body>
    <abp-modal-footer>
        <button class="btn-secondary btn" data-dismiss="modal" type="button">Close</button>
    </abp-modal-footer>
</abp-modal>